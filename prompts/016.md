# 016

- bbn crate の build-html subcommand は build-json subcommand の結果を入力として HTML を生成するサブコマンドです
- build-html を実装していくのですが、そのために次は prompts/015-result.md をベースに今回の build-html の構成を検討します
- 方針
    - TypeScript ではなく Rust を使用します
    - server を実装せず、 static site generator にします
    - virtual dom などを使用せず、単純な HTML template に埋め込みます
    - 生成されるファイル群に JavaScript は含みません
        - これは将来的には変更するかもしれませんが、 HTML + CSS だけで参照可能なサイトを構築します
- 不明点があれば、確認してください
- 検討結果を prompts/016-result.md にまとめてください、次回の実行時の計画に使用します
- この作業についての git の commit メッセージの案を英語で prompts/016-commit.md に記載してください
