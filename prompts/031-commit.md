Add --verbose flag to build command to print written file paths

Print each written file path to stdout when `bbn build --verbose` is used.
Applies to both JSON and HTML output, including static files.
