# Commit message

```
Add related.json output to build-json subcommand

Generate related.json and related/index.json for each entry in the
build-json output. Each file contains inbound links, outbound links,
next/prev entries (up to 4), and same-day entries, matching the format
previously produced by kraken.
```
