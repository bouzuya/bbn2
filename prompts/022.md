# 022

- bbn crate の command::Subcommand の各 variant の field を struct に変えて各 mod に配置してください
- 例えば `Subcommand::BuildHtml { out_dir: PathBuf }` は `Subcommand::BuildHtml(build_html::Command)` にしてください
- `build_html::run` のようなものを `build_html::Command::handle` にしてください
- この作業についての git の commit メッセージの案を英語で prompts/022-commit.md に記載してください
